/*globals describe, it, expect*/
const solution = require('../solution')

describe('', () => {
  describe('Track 1', () => {
    it('should be less than 3 hours for the am session', () => {
      expect(solution.schedule).toBeLessThan()
    })
    it('should be less than 4 hours for the pm session', () => {
      expect(solution.schedule).toBeLessThan()
    })
  })
  describe('Track 2', () => {
    it('should be less than 3 hours for the am session', () => {
      expect(solution.schedule).toBeLessThan()
    })
    it('should be less than 4 hours for the pm session', () => {
      expect(solution.schedule).toBeLessThan()
    })
  })
})
